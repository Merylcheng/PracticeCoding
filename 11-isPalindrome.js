// Challenge: 11-isPalindrome
// Difficulty: Intermediate
// Prompt:
// - Write a function called isPalindrome that accepts a single string argument, then returns true or false depending upon whether or not the string is a palindrome.
// - A palindrome is a word or phrase that are the same forward or backward.
// - Casing and spaces are not included when considering whether or not a string is a palindrome.
// - If the length of the string is 0 or 1, return true.
// Examples:
// isPalindrome('SEI Rocks'); //=> false
// isPalindrome('rotor'); //=> true
// isPalindrome('A nut for a jar of tuna'); //=> true
// isPalindrome(''); //=> true

//function accept single string arg
//check your str
//start with something simple first and understand method. console.log lowerCaseStr and turn it to all lowerrcase https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/toLowerCase
//length of the string is 0 or 1, return true. to solve the spaces, use trim

// function isPalindrome(str) {
//   console.log("working");
//   console.log(str);
// }

// isPalindrome("HI");

// function isPalindrome(str) {
//   let lowercaseStr = str.toLowerCase();
//   let trimStr = lowercaseStr.trim();
//   console.log(trimStr);
//   if (trimStr.length ===0 || trimStr.length ==)
// }

// isPalindrome("  HI i am Meryl  ");

//Questions to ask .
//why must use trim? no space in front and and back. or is it we assuming that there might be
